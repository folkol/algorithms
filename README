This repo contains a bunch of algorithms implemented of kata reasons.
(Also, to learn c!)
Also, using mac os x specific code. (Timing code for benchmarking)

QUICKSORT:
Example: gcc -std=c99 quicksort.c
for i in {1..1000000}; do echo $[ ( $RANDOM  )  + 1 ]; done > input_numbers
cat input_numbers | ./a.out use_standard > sorted_array
cat input_numbers | ./a.out > sorted_array
